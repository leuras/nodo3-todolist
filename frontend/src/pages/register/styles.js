import styled from 'styled-components'

export const ImageCover = styled.img
.attrs(() => ({ alt: 'An application made for study and practice cybersecurity in the SPA\'s universe' }))`
  width: 50vw;
  width: 45vw;
`