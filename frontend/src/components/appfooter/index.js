import React from 'react'

import * as Styled from './styles'

const AppFooter = () => {
  return (
    <Styled.AppFooter>Made with <Styled.LoveSymbol /> by Leuras | v1.0.0</Styled.AppFooter>
  )
}

export default AppFooter